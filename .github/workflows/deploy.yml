name: "Deploy to Production"

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v7.0.0
            - name: Test bash
              run: | 
                echo "Hello world"
                ls -la
